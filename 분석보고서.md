# Mass_stacker 코드 분석

분석일: 2026-09-05  
저장소: https://github.com/yuxa94/Mass_stacker  
분석 커밋: `14208d6`  
로컬 위치: `Mass_stacker/`

## 종합 평가

대지 조건으로 건물의 개략적인 입체 형태(매스)를 생성하고, 자연어 수정안을 비교·승인하는 건축 설계 탐색 프로토타입이다. 계산과 AI의 역할이 분리되어 있고, 제안 승인 및 되돌리기 흐름도 구현되어 있다. 다만 이동·회전된 형상의 면적 계산, 상태 검증, 비동기 요청의 일관성을 보강해야 한다. 현재 코드만으로 실제 인허가 적합성을 판정하는 제품이라고 볼 수는 없다. 이 보고서는 법령 검토가 아닌 구현 분석이다.

## 기술 구조

| 영역 | 구현 | 역할 |
|---|---|---|
| 화면 | index.html, style.css, app.js | 순수 HTML/CSS/ES 모듈, UI 이벤트와 상태 연결 |
| 3D | src/render/mass-viewport.js | Three.js 및 OrbitControls, 비교 뷰와 GPU 자원 정리 |
| 계산 | src/domain | 매스 생성, 변경 시뮬레이션, 지표, 검증, 대안 생성 |
| 상태 | src/state | 활성안, 제안, REV 이력, 승인·취소·되돌리기 |
| 서버 | server.js, src/server | Express 정적 서버 및 Gemini/Anthropic API 호출 |
| 저장 | 브라우저 localStorage | 프로젝트와 진행 중 제안 저장; DB 및 계정 없음 |
| 검증 | tests | Node 내장 테스트 러너 |

Three.js 0.160.0은 import map으로 unpkg에서 가져오며 글꼴도 외부에서 로드한다. package.json에는 Express와 dotenv만 있고 별도 빌드 과정은 없다.

## 사용자 흐름

1. 대지면적, 건폐율, 용적률, 최대 층수, 층고 등을 JSON으로 입력한다.
2. 규칙 기반 엔진이 면적 상한까지 직사각형 층을 쌓는다. 치수는 소수점 둘째 자리에서 내림하므로 목표 연면적과 완전히 같지는 않을 수 있다.
3. 화면에서 면적·비율·층수·높이와 층별 정보를 확인한다.
4. 균형안, 셋백형, 포디움-타워형을 규칙으로 생성해 비교·채택한다. AI는 대안의 설명을 담당한다.
5. 자연어 편집은 AI가 변경 명령으로 변환한다. 클라이언트가 검증하고 원본 복제본에 적용해 미리보기를 만든다.
6. 원본·제안 겹쳐 보기 후 승인하면 REV에 기록된다. 수정 요청은 최초 원본을 기준으로 다시 계산한다.

AI 변경 명령은 크기, 이동, 회전, 층고, 용도, 지상층 추가, 지하층 추가, 층 삭제의 8종이다. 질문 및 설명만 하는 응답도 지원한다.

## 좋은 점

- 숫자 계산을 LLM에 맡기지 않고 순수 함수로 수행한다.
- AI 응답에 구조화된 스키마를 사용하고 변경 전후 검증을 수행한다.
- 승인 전 원본을 유지하고, 승인·취소·되돌리기 및 제안 복원을 지원한다.
- 선택적인 AI 설명 호출이 실패해도 비교·채택 흐름을 유지한다.
- 렌더러가 재사용 가능한 모듈로 분리되어 있으며 자원 해제 코드가 있다.
- 계산·상태·서버 응답 정규화에 대한 테스트가 있다.

## 주요 발견과 권장 수정

### 1. 이동·회전된 층의 투영 범위를 면적 지표가 반영하지 않음 — 우선순위 높음

`src/domain/metrics.js:11`은 buildArea를 층별 width × depth 중 최댓값으로 계산한다. offsetX, offsetZ, rotationDeg는 사용하지 않는다.

재현: 10×10m인 두 층을 X축으로 20m 떨어뜨리면 평면 투영 합집합은 200㎡이나 buildArea는 100㎡로 나온다. 층들이 같은 중심에서 포개진 초기 형태에서는 성립할 수 있지만 이동·회전을 지원하는 전체 편집 범위에는 맞지 않는다.

권장: 층별 회전·이동을 반영한 다각형의 투영 합집합을 계산하고, 대지 경계 포함 여부도 별도로 검사한다. 실제 법정 면적의 예외 규정은 별도의 도메인 요구사항으로 정의해야 한다.

### 2. 대지 입력 범위와 생성 가능한 층 크기가 불일치 — 재현 완료

`validators.js`는 대지면적 1~1,000,000㎡를 허용하지만 층의 폭·깊이는 3~300m만 허용한다. `generateMaxMassSpec`은 이 층 크기 제한을 적용하지 않고, `app.js:335`의 초기 생성 경로도 결과 검증을 하지 않는다.

재현: 건폐율 60%, 용적률 200%, 최대 10층, 층고 3.3m에서 대지면적을 1 또는 1,000,000으로 주면 validateSite는 통과하고 생성 결과의 validateSpec은 실패한다. 작은 잔여 최상층에서도 같은 종류의 문제가 가능하다.

영향: 처음 표시·저장한 모델이 이후 편집 검증이나 복원에서 거부될 수 있다.

권장: 생성기와 검증기의 범위를 통일하고, 결과 검증을 통과한 모델만 프로젝트 상태로 저장한다. maxFloors 정수 여부와 대지 치수·면적 간 일관성도 검증한다.

### 3. AI 응답 도착 시 원본 상태를 확인하지 않음 — 코드상 확인

`app.js:660`의 sendChat은 요청 중 전송 버튼을 비활성화하지만 요청 ID/기준 REV 확인이 없다. 첫 제안의 경우 응답 도착 후 currentSpec()을 기준으로 시뮬레이션한다. 사용자가 그 사이 새 매스를 생성하거나 대안을 채택하면 이전 모델에 대한 AI 명령을 새 모델에 적용할 수 있다.

또한 초과 대안 설명은 proposal.id만 비교하지만 replaceProposal은 id를 유지한다. 따라서 수정 전의 늦은 설명이 수정 후 패널에 표시될 수 있다.

권장: 요청 시작 시 spec, 활성안 ID, REV, 제안 수정 번호를 캡처하고 일치하는 응답만 반영한다. AbortController 및 요청 타임아웃을 추가한다. 이 항목은 브라우저에서 지연 응답을 이용한 통합 재현까지 수행한 것은 아니다.

### 4. 저장 상태 복원 검증이 불완전 — 일부 재현 완료

`project-store.js`의 loadProject는 빈 alternatives 배열이나 존재하지 않는 activeAlternativeId를 거부하지 않는다. 최소한의 잘못된 저장 객체를 전달했을 때 null 대신 프로젝트 객체를 반환하는 것을 재현했다.

validateSpec도 site, derived, offset, rotation 등의 전체 구조를 확인하지 않는다. 제안 복원은 baseSpec/previewSpec만 확인하고 제안이 활성안과 일치하는지는 검사하지 않는다.

권장: 프로젝트 스키마 전체와 활성안 존재 여부를 검사하고 계산 지표는 원본 데이터에서 재계산한다. localStorage 읽기·쓰기 예외와 용량 초과도 처리한다. REV마다 전체 과거 대안 스냅샷을 보관하므로 이력 크기 제한이 필요하다.

### 5. 외부 공개용 서버 방어가 부족함 — 배포 전 개선

server.js에는 인증, 호출량 제한, 모델 허용 목록 및 외부 API 호출 타임아웃이 없다. 접근 가능한 사람이 서버의 AI 키를 사용하는 요청을 보낼 수 있다. app.listen은 호스트를 명시하지 않는다. 저장소 루트를 정적 서빙하므로 서버 코드·문서 등도 공개 대상에 포함된다. 단, 이것만으로 .env가 노출된다고 판단해서는 안 된다.

권장: 개인 실행은 localhost 바인딩을 명시하고, 공개 배포는 인증·요청 제한·모델 제한을 추가한다. 정적 파일은 public 디렉터리로 분리하고 상세 디버그 응답은 개발 모드에서만 제공한다.

## 검증 범위

- 저장소 다운로드 및 주요 코드·개발 문서 검토 완료.
- `node --test`: 총 33개 통과, 실패 0개.
- 추가 임시 Node 실행으로 입력/생성 검증 불일치, 이동층 면적 계산, 빈 프로젝트 복원 재현.
- 기존 소스와 테스트는 수정하지 않음.
- 의존성 설치, 웹 서버 실행, 브라우저 시각 검증, 실제 유료 LLM 호출은 수행하지 않음.
- tests/server는 프롬프트·스키마·정규화 검증이며 실제 HTTP 및 외부 API 통합 검증과는 구별해야 함.

## 실행 방법

Node.js에서 서버 코드의 내장 fetch를 사용할 수 있어야 한다. 저장소에서 다음 명령으로 의존성을 설치하고 시작한다.

```powershell
cd Mass_stacker
npm ci
Copy-Item .env.example .env
# .env에 사용할 공급자의 API 키와 사용 가능한 모델 ID 설정
npm start
```

기본 주소는 http://localhost:8790 이다. 규칙 기반 생성은 AI 키 없이 가능하지만 AI 편집·설명은 키가 필요하다. 코드에 기재된 기본 모델 ID의 실제 공급자 지원 여부는 이번 분석에서 확인하지 않았다.

## 권장 진행 순서

1. 생성/검증 범위 일치 및 투영 면적 계산 개선.
2. 비동기 요청의 기준 모델·REV 검증과 저장 상태 검증 보강.
3. 위 재현 사례의 회귀 테스트 및 브라우저 통합 테스트 추가.
4. 외부 배포가 필요하면 서버 접근 제어와 정적 파일 분리.
5. 제품 확장 단계에서 프로젝트 파일 내보내기/가져오기, 사용자별 저장, 대지 다각형 및 추가 설계 제약 도입.
